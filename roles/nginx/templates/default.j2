server {
        listen {{ port }} default_server;
        listen [::]:{{ port }};
        root /var/www/{{ directory }};
        index index.html index.htm index.nginx-debian.html;

        server_name {{ domain }};

        location / {
       }
}
